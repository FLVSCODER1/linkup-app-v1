# LinkUp release notes

Each versioned push adds a Markdown file named after its semantic version.
Prerelease builds use identifiers such as `beta.3`; stable releases use the
standard `major.minor.patch` format.
